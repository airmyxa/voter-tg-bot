use crate::{
    commands::command::Command, commands::command_dispatcher::CommandDispatcher,
    components_container::ComponentsContainer,
};

// pub fn add_components(components_container: &mut ComponentsContainer) {
//    let _ = components_container
//        .get_component_as::<CommandDispatcher>(CommandDispatcher::component_name());
// }
